services:
  backend:
    build: ./backend
    container_name: chatwar-backend
    restart: unless-stopped
    env_file: .env
    ports:
      - "3000:3000"
    networks:
      - chatwar
      - public_databases

networks:
  chatwar:
    name: chatwar
    driver: bridge
  public_databases:
    external: true
